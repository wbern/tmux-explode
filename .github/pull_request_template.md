<!--
Thanks for the contribution. A few quick checks:

- Subject follows Conventional Commits (`feat:`, `fix:`, etc.). The
  commit-msg hook enforces this — see CONTRIBUTING.md for the full list.
- New behaviour is covered by a scenario in `tests/visual.sh`.
- `shellcheck -S warning scripts/*.sh tests/*.sh hooks/commit-msg` is clean.
- README updated if a user-facing option or default changed.
-->

## What

<!-- One or two sentences. -->

## Why

<!-- Motivation. Link to an issue if there is one. -->

## How tested

<!--
- Which `./tests/visual.sh` scenarios cover it?
- Did you exercise it manually with `./tests/demo.sh`? On what tmux version?
-->
