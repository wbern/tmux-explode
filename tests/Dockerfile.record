FROM debian:bookworm-slim
RUN apt-get update \
 && apt-get install -y --no-install-recommends tmux bash procps coreutils ca-certificates \
 && rm -rf /var/lib/apt/lists/*
WORKDIR /repo
COPY . /repo
ENV TERM=xterm-256color
CMD ["/repo/tests/record_session.sh"]
